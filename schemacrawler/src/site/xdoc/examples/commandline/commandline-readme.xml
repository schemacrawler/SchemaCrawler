<!DOCTYPE document PUBLIC "-//Apache Software Foundation//DTD XDOC 1.0//EN"
  "http://maven.apache.org/dtd/xdoc_1_0.dtd">
<document>
  <properties>
    <title>SchemaCrawler - Commandline Example</title>
    <author email="sualeh@hotmail.com">Sualeh Fatehi</author>
  </properties>
  <body>
    <section name="SchemaCrawler - Commandline Example">
      <subsection name="Description">
        <p>The command example demonstrates the use of SchemaCrawler from
          the
          shell command line.
        </p>
      </subsection>
      <subsection name="How to Run">
        <ol>
          <li>
            Make sure that
            <span class="code">java</span>
            is on your
            <span class="code">PATH</span>
          </li>
          <li>
            Start the database server by running the
            <span class="code">StartDatabaseServer</span>
            script from the
            distribution directory
          </li>
          <li>
            Start a command shell in the
            <em>commandline</em>
            example directory.
          </li>
          <li>
            Run
            <span class="code">sc.cmd -help</span>
            (or
            <span class="code">sc.sh -help</span>
            on Unix), to give you a list of available command line
            options
          </li>
          <li>
            To start with, run
            <span class="code">sc.cmd -infolevel=minimum -command=list</span>
            (or
            <span class="code">sc.sh -infolevel=minimum -command=list</span>
            on Unix); typically, connection parameters would need to provided,
            but in the examples, these are built into the sc.cmd and sc.sh scripts
          </li>
        </ol>
      </subsection>
      <subsection name="How to Experiment">
        <ol>
          <li>
            Take a look at the SchemaCrawler grep example, in the
            <a href="../grep/grep-readme.html">
              <code>grep</code>
            </a>
            example directory.
          </li>
          <li>
            Take a look at the SchemaCrawler lint example, in the
            <a href="../lint/lint-readme.html">
              <code>lint</code>
            </a>
            example directory.
          </li>
          <li>Try using different SchemaCrawler command line options.</li>
          <li>
            Try running an arbitrary query, with a query command
            <code>"-command=SELECT * FROM PUBLIC.BOOKS.AUTHORS"</code>
            . (The quotes are required.)
          </li>
          <li>
            Redirect the output of SchemaCrawler into a file, by running
            <span class="code">sc.cmd &gt; file.txt</span>
            .
          </li>
          <li>Try defining a new database connection, using your favorite
            database and driver.
          </li>
        </ol>
      </subsection>
    </section>
  </body>
</document>
