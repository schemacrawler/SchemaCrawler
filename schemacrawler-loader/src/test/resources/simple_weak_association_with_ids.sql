CREATE TABLE BOOK
(
  ID INTEGER NOT NULL,
  TITLE CHAR(25),
  PRIMARY KEY (ID)
);

CREATE TABLE BOOK2
(
  BOOKID INTEGER NOT NULL,
  TITLE CHAR(25),
  PRIMARY KEY (BOOKID)
);

CREATE TABLE BOOK3
(
  BOOK INTEGER NOT NULL,
  TITLE CHAR(25),
  PRIMARY KEY (BOOK)
);

CREATE TABLE READER1
(
  ID INTEGER NOT NULL,
  BOOKID INTEGER,
  NAME CHAR(25),
  PRIMARY KEY (ID)
);

CREATE TABLE READER2
(
  ID INTEGER NOT NULL,
  BOOK INTEGER,
  NAME CHAR(25),
  PRIMARY KEY (ID)
);
