database-connector:
  database-server-type:
    server: adapter-test
    name: Adapter Test
  url-template: "jdbc:adapter-test:${database}"
  additional-options:
    - name: sampleSetting
      type: string
      default: sample
      help:
        - Sample setting
