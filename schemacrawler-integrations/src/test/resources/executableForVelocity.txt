-- generated by: SchemaCrawler 8.9

-- database: HSQL Database Engine 2.2.6

-- driver: HSQL Database Engine Driver 2.2.6
-- driver class: org.hsqldb.jdbc.JDBCDriver
-- url: jdbc:hsqldb:mem:schemacrawler
-- jdbc compliant: true

SET SCHEMA PUBLIC.BOOKS;

CREATE TABLE AUTHORS
(
  ADDRESS1 VARCHAR,
  ADDRESS2 VARCHAR,
  CITY VARCHAR,
  COUNTRY VARCHAR,
  FIRSTNAME VARCHAR NOT NULL,
  ID INTEGER NOT NULL,
  LASTNAME VARCHAR NOT NULL,
  POSTALCODE VARCHAR,
  STATE VARCHAR
);

CREATE TABLE BOOKAUTHORS
(
  AUTHORID INTEGER NOT NULL,
  BOOKID INTEGER NOT NULL,
  "UPDATE" CLOB
);

CREATE TABLE BOOKS
(
  DESCRIPTION VARCHAR,
  ID INTEGER NOT NULL,
  PRICE DOUBLE,
  PUBLICATIONDATE DATE,
  PUBLISHERID INTEGER NOT NULL,
  TITLE VARCHAR NOT NULL
);

CREATE TABLE "Global Counts"
(
  "Global Count" INTEGER
);

CREATE TABLE PUBLISHERS
(
  ID INTEGER NOT NULL,
  PUBLISHER VARCHAR
);

SET SCHEMA PUBLIC.FOR_LINT;

CREATE TABLE "Global Counts"
(
  "Global Count" INTEGER
);

CREATE TABLE No_Columns
(

);

CREATE TABLE PUBLICATIONS
(
  DESCRIPTION VARCHAR,
  ID INTEGER NOT NULL,
  PRESS_RELEASE CLOB,
  PRICE DOUBLE,
  PUBLICATIONDATE DATE,
  TITLE VARCHAR NOT NULL,
  "UPDATE" CLOB,
  WRITERID INTEGER NOT NULL
);

CREATE TABLE WRITERS
(
  ADDRESS1 VARCHAR,
  ADDRESS2 VARCHAR NOT NULL,
  CITY VARCHAR,
  COUNTRY VARCHAR,
  EMAIL1 VARCHAR,
  EMAIL2 INTEGER,
  FAX VARCHAR,
  FAX3 INTEGER,
  FIRSTNAME VARCHAR NOT NULL,
  HOMEEMAIL11 VARCHAR,
  HOMEEMAIL12 VARCHAR,
  ID INTEGER NOT NULL,
  LASTNAME VARCHAR NOT NULL,
  PHONE1 VARCHAR,
  PHONE2 VARCHAR,
  POSTALCODE VARCHAR,
  PUBLICATION_ID BIGINT NOT NULL,
  STATE VARCHAR
);

SET SCHEMA PUBLIC.INFORMATION_SCHEMA;

SET SCHEMA PUBLIC.PUBLIC;

SET SCHEMA PUBLIC."PUBLISHER SALES";

CREATE TABLE REGIONS
(
  CITY VARCHAR NOT NULL,
  COUNTRY VARCHAR NOT NULL,
  POSTALCODE VARCHAR NOT NULL,
  STATE VARCHAR NOT NULL
);

CREATE TABLE SALES
(
  BOOKID INTEGER,
  COUNTRY VARCHAR NOT NULL,
  PERIODENDDATE DATE,
  POSTALCODE VARCHAR NOT NULL,
  TOTALAMOUNT DOUBLE
);

SET SCHEMA PUBLIC.SYSTEM_LOBS;

